const StorePage = function() {
  const womenBtn = element(by.css('a[title*="women"]'));
  const products = element.all(by.css('.product_img_link'));
  const addToCart = element(by.css('a[title="Add to cart"]'));
}
